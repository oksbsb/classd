#!/bin/dash

FLAG_FILE=/tmp/untangle-classd.flag
LOG_PATH=/var/log/untangle-classd

# we only want to delete the log and flag if
# the package is being completely removed

if [ "$1" = "purge" ]; then
    rm -f $LOG_PATH/classd.*
    rm -f $LOG_PATH/*.log
    rm -f $FLAG_FILE
fi

exit 0
