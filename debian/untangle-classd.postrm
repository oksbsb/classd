#!/bin/bash

CLASSD_LOG_PATH=/var/log/untangle-classd

if [ -f /etc/default/untangle-classd ]; then
    . /etc/default/untangle-classd
fi

case $1 in
  purge)
    rm -f $CLASSD_LOG_PATH/classd.*
    ;;
esac

exit 0

